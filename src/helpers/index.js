const _requestHelper = require("./request.helper");
const _responseHelper = require("./response.helper");

const helpers = {
    responseHelper: _responseHelper,
    requestHelper: _requestHelper
}

module.exports = helpers;